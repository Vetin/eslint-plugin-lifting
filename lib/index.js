const { createRule } = require('./create-rule');
const { collectIdentifiers } = require('./collect-identifiers');
const { capitalize } = require('./capitalize');

module.exports = { createRule, collectIdentifiers, capitalize };
